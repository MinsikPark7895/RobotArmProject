version: '3.8'

services:
  yolo_dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yolo_dev
    tty: true
    volumes:
      - ./yolo_dev:/app
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]